---
// Reasons.astro — Short labelled rows that break a decision into chunks,
// e.g. Problem / Decision / Why / Trade-off. Wrap <Reason> rows.
---

<div class="cs-block cs-reasons" data-cs-reveal>
  <slot />
</div>
