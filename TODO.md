# Design System & Transitions Polish — TODO

## Goal
Sync the transitions.dev skills with the UI polish agent and apply safer, production-ready, theme-aware transitions.

## Steps
- [x] 1. Add the shared motion-token scale to `src/styles.css`
- [x] 2. Apply the **tabs sliding** transition to the Projects tab bar (ProjectsSection.astro + styles.css)
- [x] 3. Apply the **texts reveal** transition to the hero intro (PortfolioIntro.astro + styles.css)
- [x] 4. Apply the **icon swap** transition to ThemeToggle + add moon icon (ThemeToggle.astro + moon.svg)
- [x] 5. Sync `ui-polish.agent.md` to reference the transitions skills (N/A — no such file exists; project uses `skills-lock.json`)
- [x] 6. Verify with `npm run build` (✓ build passed)

